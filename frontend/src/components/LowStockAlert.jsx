// export default function LowStockAlert({ lowStock }) {
//   return (
//     <div>
//       <h3>Low Stock Alert</h3>
//       {lowStock.length === 0 ? (
//         <p>All stores have sufficient stock</p>
//       ) : (
//         <ul>
//           {lowStock.map((s, i) => (
//             <li key={i}>{s.store} has low stock</li>
//           ))}
//         </ul>
//       )}
//     </div>
//   );
// }
